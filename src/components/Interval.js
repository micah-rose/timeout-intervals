import React from 'react';
import classes from './Component.css';

const Interval = () => {
    return (
        <div className={classes.Interval}>
        <h1>Interval</h1>
        <p>To demonstrate how setInterval works...</p>
    </div>
    )
}

export default Interval;